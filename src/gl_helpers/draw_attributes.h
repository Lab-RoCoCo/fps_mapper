#pragma once

typedef int DrawAttributesType;

#define ATTRIBUTE_SHOW     0x1  // default, shows an object
#define ATTRIBUTE_SELECTED 0x2  // policy for a selected object
#define ATTRIBUTE_HIDE_REF 0x4	// if set, the (local) reference system is not displayed and the position of the keyframes will also be hidden

